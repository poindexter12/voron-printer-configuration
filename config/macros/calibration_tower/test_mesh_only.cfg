[gcode_macro TEST_MESH_ONLY]
description: Test macro that calls the calibration tower with 0 blocks (mesh only)
gcode:
    ; Test the mesh-only functionality
    CONFIGURATION_TOWER CALIBRATION_BLOCKS=0
